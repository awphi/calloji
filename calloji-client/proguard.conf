-dontoptimize

# Save meta-data for stack traces
-renamesourcefileattribute SourceFile
-keepattributes SourceFile,LineNumberTable

# Rename FXML files together with related views
-adaptresourcefilenames **.fxml,**.png,**.css
-adaptresourcefilecontents **.fxml
-adaptclassstrings

# Keep all annotations and meta-data
-keepattributes *Annotation*,Signature,EnclosingMethod

# Keep entry-point class
-keep class ph.adamw.calloji.client.Client {
    public static void main(java.lang.String[]);
}

# TODO find a way around keeping the packet and util classes - possibly proguard them in the sync module then build that into server and client?
-keep class ph.adamw.calloji.packet.** {
  *;
}

-keep class ph.adamw.calloji.util.** {
   *;
}

# Keep all classes inside application
-keep,allowobfuscation class ph.adamw.calloji.client.** {
}

# Keep names of fields marked with @FXML attribute
-keepclassmembers class * {
    @javafx.fxml.FXML *;
}